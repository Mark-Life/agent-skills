{
  "name": "agent-skills",
  "private": true,
  "type": "module",
  "description": "Dev/test harness for the agent-skills repo. NOT shipped — skills install standalone via the skills CLI.",
  "scripts": {
    "test": "bun test tests",
    "test:guard": "bun test tests/guard.test.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
