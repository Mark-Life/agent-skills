---
name: Agent Workflow Notes
description: How the team runs parallel coding agents across git worktrees
type: project
originSessionId: 77777777-7777-4777-8777-777777777777
---

This file uses the legacy flat frontmatter shape: `type` and `originSessionId` sit at the top level with no `metadata:` wrapper and no `node_type` (SCH11). Its Title-Case `name` ("Agent Workflow Notes") does not match the filename stem `project_agent_workflow` (SCH08), and the snake_case filename violates the kebab-case convention (IDX09). The curator's migration path rewrites this into the nested schema.
